create or replace TRANSIENT TABLE TRANSIENT.EMPLOYEE_DEFAULT_EXPRESSION_147 (
	EMP_ID NUMBER(10,0),
	EMP_NAME VARCHAR(100),
	SALARY NUMBER(10,2),
	BONUS NUMBER(10,2) DEFAULT 4600 + 1000,
	TOTAL_AMOUNT NUMBER(10,2) DEFAULT 15000 * 2,
	CITY VARCHAR(50)
);