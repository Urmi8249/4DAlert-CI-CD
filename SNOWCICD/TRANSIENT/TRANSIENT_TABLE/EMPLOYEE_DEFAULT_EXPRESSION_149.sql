create or replace TRANSIENT TABLE TRANSIENT.EMPLOYEE_DEFAULT_EXPRESSION_149 (
	EMP_ID NUMBER(10,0),
	EMP_NAME VARCHAR(100),
	SALARY NUMBER(10,2),
	BONUS NUMBER(10,2) DEFAULT 4600 + 1000,
	CITY VARCHAR(50)
);