create or replace TABLE SOUMYAA_1.EMPLOYEE_DEFAULT_EXPRESSION (
	EMP_ID NUMBER(10,0),
	EMP_NAME VARCHAR(100),
	SALARY NUMBER(10,2),
	BONUS NUMBER(10,2) DEFAULT 5000 + 1000,
	TOTAL_AMOUNT NUMBER(10,2) DEFAULT 10000 * 2,
	CITY VARCHAR(50)
);